export interface User {
    //interfaces in typescript are different from interfaces in c#
    //interfaces in typescript, we can use it to specify that something is a type of something
    username: string;
    token: string;
}
